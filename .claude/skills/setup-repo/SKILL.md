# Setup Repo Skill
1. Detect current branch with `git branch --show-current`.
2. Ask which GitHub account (personal/work) to use.
3. Set remote with the matching SSH host alias.
4. Push using the actual branch name (never assume main).
